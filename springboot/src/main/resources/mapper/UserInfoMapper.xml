<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.xjtu.springboot.mapper.UserInfoMapper">
    <resultMap id="BaseResultMap" type="com.xjtu.springboot.pojo.UserInfo">
        <id column="id" jdbcType="BIGINT" property="id"/>
        <result column="user_id" jdbcType="BIGINT" property="userId"/>
        <result column="username" jdbcType="VARCHAR" property="username"/>
        <result column="avatar" jdbcType="VARCHAR" property="avatar"/>
        <result column="email" jdbcType="VARCHAR" property="email"/>
        <result column="birthday" jdbcType="TIMESTAMP" property="birthday"/>
        <result column="sex" jdbcType="TINYINT" property="sex"/>
        <result column="status" jdbcType="TINYINT" property="status"/>
        <result column="bio" jdbcType="VARCHAR" property="bio"/>
        <result column="pwd_failed_count" jdbcType="TINYINT" property="pwdFailedCount"/>
        <result column="last_login_time" jdbcType="TIMESTAMP" property="lastLoginTime"/>
        <result column="last_login_ip" jdbcType="VARCHAR" property="lastLoginIp"/>
        <result column="created_at" jdbcType="TIMESTAMP" property="createdAt"/>
        <result column="updated_at" jdbcType="TIMESTAMP" property="updatedAt"/>
    </resultMap>
    <delete id="deleteByPrimaryKey" parameterType="java.lang.Long">
        delete
        from user_info
        where id = #{id,jdbcType=BIGINT}
    </delete>
    <update id="updateByPrimaryKey" parameterType="com.xjtu.springboot.pojo.UserInfo">
        update user_info
        set user_id          = #{userId,jdbcType=BIGINT},
            username         = #{username,jdbcType=VARCHAR},
            avatar           = #{avatar,jdbcType=VARCHAR},
            email            = #{email,jdbcType=VARCHAR},
            birthday         = #{birthday,jdbcType=TIMESTAMP},
            sex              = #{sex,jdbcType=TINYINT},
            status           = #{status,jdbcType=TINYINT},
            bio              = #{bio,jdbcType=VARCHAR},
            pwd_failed_count = #{pwdFailedCount,jdbcType=TINYINT},
            last_login_time  = #{lastLoginTime,jdbcType=TIMESTAMP},
            last_login_ip    = #{lastLoginIp,jdbcType=VARCHAR},
            created_at       = #{createdAt,jdbcType=TIMESTAMP},
            updated_at       = #{updatedAt,jdbcType=TIMESTAMP}
        where id = #{id,jdbcType=BIGINT}
    </update>
    <select id="selectByPrimaryKey" parameterType="java.lang.Long" resultMap="BaseResultMap">
        select id,
               user_id,
               username,
               avatar,
               email,
               birthday,
               sex,
               status,
               bio,
               pwd_failed_count,
               last_login_time,
               last_login_ip,
               created_at,
               updated_at
        from user_info
        where id = #{id,jdbcType=BIGINT}
    </select>
    <select id="selectAll" resultMap="BaseResultMap">
        select id,
               user_id,
               username,
               avatar,
               email,
               birthday,
               sex,
               status,
               bio,
               pwd_failed_count,
               last_login_time,
               last_login_ip,
               created_at,
               updated_at
        from user_info
    </select>
    <insert id="insert" parameterType="com.xjtu.springboot.pojo.UserInfo">
        <selectKey keyProperty="id" order="AFTER" resultType="java.lang.Long">
            SELECT LAST_INSERT_ID()
        </selectKey>
        insert into user_info (user_id, username, avatar,
        email, birthday, sex, status, bio, pwd_failed_count, last_login_time,
        last_login_ip, created_at, updated_at
        )
        values (#{userId,jdbcType=BIGINT}, #{username,jdbcType=VARCHAR}, #{avatar,jdbcType=VARCHAR},
        #{email,jdbcType=VARCHAR}, #{birthday,jdbcType=TIMESTAMP},
        #{sex,jdbcType=TINYINT},#{status,jdbcType=TINYINT},
        #{bio,jdbcType=VARCHAR},
        #{pwdFailedCount,jdbcType=TINYINT},
        #{lastLoginTime,jdbcType=TIMESTAMP},
        #{lastLoginIp,jdbcType=VARCHAR}, #{createdAt,jdbcType=TIMESTAMP},
        #{updatedAt,jdbcType=TIMESTAMP}
        )
    </insert>

    <select id="selectByUserId" parameterType="Long" resultMap="BaseResultMap">
        select id,
               user_id,
               username,
               avatar,
               email,
               birthday,
               sex,
               status,
               bio,
               pwd_failed_count,
               last_login_time,
               last_login_ip,
               created_at,
               updated_at
        from user_info
        where user_id = #{id}
    </select>
</mapper>