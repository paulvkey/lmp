<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.xjtu.springboot.mapper.UserProfileMapper">
    <resultMap id="BaseResultMap" type="com.xjtu.springboot.pojo.UserProfile">
        <id column="id" jdbcType="BIGINT" property="id"/>
        <result column="user_id" jdbcType="BIGINT" property="userId"/>
        <result column="username" jdbcType="VARCHAR" property="username"/>
        <result column="avatar" jdbcType="VARCHAR" property="avatar"/>
        <result column="sex" jdbcType="SMALLINT" property="sex"/>
        <result column="email" jdbcType="VARCHAR" property="email"/>
        <result column="birthday" jdbcType="TIMESTAMP" property="birthday"/>
        <result column="bio" jdbcType="VARCHAR" property="bio"/>
        <result column="last_login_time" jdbcType="TIMESTAMP" property="lastLoginTime"/>
        <result column="last_login_ip" jdbcType="VARCHAR" property="lastLoginIp"/>
        <result column="created_at" jdbcType="TIMESTAMP" property="createdAt"/>
        <result column="updated_at" jdbcType="TIMESTAMP" property="updatedAt"/>
    </resultMap>

    <delete id="deleteByUserId" parameterType="java.lang.Long">
        delete
        from lmp.user_profile
        where user_id = #{userId,jdbcType=BIGINT}
    </delete>

    <update id="updateByPrimaryKey" parameterType="com.xjtu.springboot.pojo.UserProfile">
        update lmp.user_profile
        set user_id         = #{userId,jdbcType=BIGINT},
        username        = #{username,jdbcType=VARCHAR},
        avatar          = #{avatar,jdbcType=VARCHAR},
        sex             = #{sex,jdbcType=SMALLINT},
        email           = #{email,jdbcType=VARCHAR},
        birthday        = #{birthday,jdbcType=TIMESTAMP},
        bio             = #{bio,jdbcType=VARCHAR},
        last_login_time = #{lastLoginTime,jdbcType=TIMESTAMP},
        last_login_ip   = #{lastLoginIp,jdbcType=VARCHAR},
        created_at      = #{createdAt,jdbcType=TIMESTAMP},
        updated_at      = #{updatedAt,jdbcType=TIMESTAMP}
        where id = #{id,jdbcType=BIGINT}
        and user_id = #{userId,jdbcType=BIGINT}
    </update>

    <select id="selectByPrimaryKey" parameterType="java.lang.Long" resultMap="BaseResultMap">
        select id,
               user_id,
               username,
               avatar,
               sex,
               email,
               birthday,
               bio,
               last_login_time,
               last_login_ip,
               created_at,
               updated_at
        from lmp.user_profile
        where id = #{id,jdbcType=BIGINT}
    </select>

    <insert id="insert" parameterType="com.xjtu.springboot.pojo.UserProfile"
            useGeneratedKeys="true" keyColumn="id" keyProperty="id">
        <selectKey keyProperty="id" order="AFTER" resultType="java.lang.Long">
            SELECT currval(pg_get_serial_sequence('lmp.user_profile', 'id'))
        </selectKey>
        insert into lmp.user_profile (user_id, username, avatar,
        sex, email, birthday, bio,
        last_login_time,
        last_login_ip, created_at,
        updated_at
        )
        values (#{userId,jdbcType=BIGINT}, #{username,jdbcType=VARCHAR}, #{avatar,jdbcType=VARCHAR},
        #{sex,jdbcType=SMALLINT},
        #{email,jdbcType=VARCHAR}, #{birthday,jdbcType=TIMESTAMP}, #{bio,jdbcType=VARCHAR},
        #{lastLoginTime,jdbcType=TIMESTAMP},
        #{lastLoginIp,jdbcType=VARCHAR}, #{createdAt,jdbcType=TIMESTAMP},
        #{updatedAt,jdbcType=TIMESTAMP}
        )
    </insert>

    <select id="selectByUserId" parameterType="Long" resultMap="BaseResultMap">
        select id,
               user_id,
               username,
               avatar,
               sex,
               email,
               birthday,
               bio,
               last_login_time,
               last_login_ip,
               created_at,
               updated_at
        from lmp.user_profile
        where user_id = #{userId}
    </select>
</mapper>