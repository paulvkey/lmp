<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.xjtu.springboot.mapper.UserCollectionMapper">
    <resultMap id="BaseResultMap" type="com.xjtu.springboot.pojo.UserCollection">
        <id column="id" jdbcType="BIGINT" property="id"/>
        <result column="user_id" jdbcType="BIGINT" property="userId"/>
        <result column="session_id" jdbcType="BIGINT" property="sessionId"/>
        <result column="session_title" jdbcType="VARCHAR" property="sessionTitle"/>
        <result column="collection_note" jdbcType="VARCHAR" property="collectionNote"/>
        <result column="collected_at" jdbcType="TIMESTAMP" property="collectedAt"/>
    </resultMap>
    <delete id="deleteByPrimaryKey" parameterType="java.lang.Long">
        delete
        from user_collection
        where id = #{id,jdbcType=BIGINT}
    </delete>
    <insert id="insert" parameterType="com.xjtu.springboot.pojo.UserCollection" keyColumn="id"
            keyProperty="id" useGeneratedKeys="true">
        <selectKey keyProperty="id" order="AFTER" resultType="java.lang.Long">
            SELECT LAST_INSERT_ID()
        </selectKey>
        insert into user_collection (user_id, session_id, session_title,
        collection_note, collected_at)
        values (#{userId,jdbcType=BIGINT}, #{sessionId,jdbcType=BIGINT},
        #{sessionTitle,jdbcType=VARCHAR},
        #{collectionNote,jdbcType=VARCHAR}, #{collectedAt,jdbcType=TIMESTAMP})
    </insert>
    <update id="updateByPrimaryKey" parameterType="com.xjtu.springboot.pojo.UserCollection">
        update user_collection
        set user_id         = #{userId,jdbcType=BIGINT},
            session_id      = #{sessionId,jdbcType=BIGINT},
            session_title   = #{sessionTitle,jdbcType=VARCHAR},
            collection_note = #{collectionNote,jdbcType=VARCHAR},
            collected_at    = #{collectedAt,jdbcType=TIMESTAMP}
        where id = #{id,jdbcType=BIGINT}
    </update>
    <select id="selectByPrimaryKey" parameterType="java.lang.Long" resultMap="BaseResultMap">
        select id, user_id, session_id, session_title, collection_note, collected_at
        from user_collection
        where id = #{id,jdbcType=BIGINT}
    </select>
    <select id="selectAll" resultMap="BaseResultMap">
        select id, user_id, session_id, session_title, collection_note, collected_at
        from user_collection
    </select>

    <select id="selectAllByUserId" resultMap="BaseResultMap">
        select id, user_id, session_id, session_title, collection_note, collected_at
        from user_collection
        where user_id = #{userId}
        order by collected_at desc
    </select>

    <delete id="deleteBySessionId" parameterType="java.lang.Long">
        delete
        from user_collection
        where session_id = #{sessionId}
    </delete>

    <select id="selectBySessionId" parameterType="java.lang.Long" resultMap="BaseResultMap">
        select id, user_id, session_id, session_title, collection_note, collected_at
        from user_collection
        where session_id = #{sessionId}
    </select>
</mapper>