<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.xjtu.springboot.mapper.ModelMapper">
    <resultMap id="BaseResultMap" type="com.xjtu.springboot.pojo.Model">
        <id column="id" jdbcType="BIGINT" property="id"/>
        <result column="name" jdbcType="VARCHAR" property="name"/>
        <result column="desc" jdbcType="VARCHAR" property="desc"/>
        <result column="is_default" jdbcType="SMALLINT" property="isDefault"/>
        <result column="status" jdbcType="SMALLINT" property="status"/>
        <result column="max_token" jdbcType="INTEGER" property="maxToken"/>
        <result column="prompt" jdbcType="VARCHAR" property="prompt"/>
    </resultMap>

    <delete id="deleteByPrimaryKey" parameterType="java.lang.Long">
        delete
        from lmp.model
        where id = #{id,jdbcType=BIGINT}
    </delete>

    <insert id="insert" parameterType="com.xjtu.springboot.pojo.Model"
            useGeneratedKeys="true" keyColumn="id" keyProperty="id">
        <selectKey keyProperty="id" order="AFTER" resultType="java.lang.Long">
            SELECT currval(pg_get_serial_sequence('lmp.model', 'id'))
        </selectKey>
        insert into lmp.model (name, "desc",
        is_default, status, max_token,
        prompt)
        values (#{name,jdbcType=VARCHAR}, #{desc,jdbcType=VARCHAR},
        #{isDefault,jdbcType=SMALLINT}, #{status,jdbcType=SMALLINT}, #{maxToken,jdbcType=INTEGER},
        #{prompt,jdbcType=VARCHAR})
    </insert>

    <update id="updateByPrimaryKey" parameterType="com.xjtu.springboot.pojo.Model">
        update lmp.model
        set name       = #{name,jdbcType=VARCHAR},
            "desc"     = #{desc,jdbcType=VARCHAR},
            is_default = #{isDefault,jdbcType=SMALLINT},
            status     = #{status,jdbcType=SMALLINT},
            max_token  = #{maxToken,jdbcType=INTEGER},
            prompt     = #{prompt,jdbcType=VARCHAR}
        where id = #{id,jdbcType=BIGINT}
    </update>

    <select id="selectByPrimaryKey" parameterType="java.lang.Long" resultMap="BaseResultMap">
        select id, name, "desc", is_default, status, max_token, prompt
        from lmp.model
        where id = #{id,jdbcType=BIGINT}
    </select>

    <select id="selectAll" resultMap="BaseResultMap">
        select id, name, "desc", is_default, status, max_token, prompt
        from lmp.model
    </select>
</mapper>