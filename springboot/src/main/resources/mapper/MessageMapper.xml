<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.xjtu.springboot.mapper.MessageMapper">
    <resultMap id="BaseResultMap" type="com.xjtu.springboot.pojo.Message">
        <id column="id" jdbcType="BIGINT" property="id"/>
        <result column="user_id" jdbcType="BIGINT" property="userId"/>
        <result column="session_id" jdbcType="BIGINT" property="sessionId"/>
        <result column="role" jdbcType="SMALLINT" property="role"/>
        <result column="thinking" jdbcType="LONGVARCHAR" property="thinking"/>
        <result column="content" jdbcType="LONGVARCHAR" property="content"/>
        <result column="type" jdbcType="SMALLINT" property="type"/>
        <result column="file_ids" jdbcType="VARCHAR" property="fileIds"/>
        <result column="token_count" jdbcType="INTEGER" property="tokenCount"/>
        <result column="is_deep_think" jdbcType="SMALLINT" property="isDeepThink"/>
        <result column="is_network_search" jdbcType="SMALLINT" property="isNetworkSearch"/>
        <result column="send_time" jdbcType="TIMESTAMP" property="sendTime"/>
    </resultMap>

    <delete id="deleteByPrimaryKey" parameterType="com.xjtu.springboot.pojo.Message">
        delete
        from lmp.message
        where user_id = #{userId,jdbcType=BIGINT}
          and session_id = #{sessionId,jdbcType=BIGINT}
          and id = #{id,jdbcType=BIGINT}
    </delete>

    <insert id="insert" parameterType="com.xjtu.springboot.pojo.Message"
            useGeneratedKeys="true" keyColumn="id" keyProperty="id">
        <selectKey keyProperty="id" order="AFTER" resultType="java.lang.Long">
            SELECT currval(pg_get_serial_sequence('lmp.message', 'id'))
        </selectKey>
        insert into lmp.message (user_id, session_id, role, thinking,
        content, type,
        file_ids, token_count, is_deep_think,
        is_network_search, send_time)
        values (#{userId,jdbcType=BIGINT}, #{sessionId,jdbcType=BIGINT},
        #{role,jdbcType=SMALLINT},#{thinking,jdbcType=LONGVARCHAR},
        #{content,jdbcType=LONGVARCHAR},#{type,jdbcType=SMALLINT},
        #{fileIds,jdbcType=VARCHAR}, #{tokenCount,jdbcType=INTEGER},
        #{isDeepThink,jdbcType=SMALLINT},
        #{isNetworkSearch,jdbcType=SMALLINT},
        #{sendTime,jdbcType=TIMESTAMP})
    </insert>

    <update id="updateByPrimaryKey" parameterType="com.xjtu.springboot.pojo.Message">
        update lmp.message
        set user_id           = #{userId,jdbcType=BIGINT},
            session_id        = #{sessionId,jdbcType=BIGINT},
            role              = #{role,jdbcType=SMALLINT},
            thinking          = #{thinking,jdbcType=LONGVARCHAR},
            content           = #{content,jdbcType=LONGVARCHAR},
            type              = #{type,jdbcType=SMALLINT},
            file_ids          = #{fileIds,jdbcType=VARCHAR},
            token_count       = #{tokenCount,jdbcType=INTEGER},
            is_deep_think     = #{isDeepThink,jdbcType=SMALLINT},
            is_network_search = #{isNetworkSearch,jdbcType=SMALLINT},
            send_time         = #{sendTime,jdbcType=TIMESTAMP}
        where id = #{id,jdbcType=BIGINT}
    </update>

    <select id="selectByIds" resultMap="BaseResultMap">
        select id,
               user_id,
               session_id,
               role,
               thinking,
               content,
               type,
               file_ids,
               token_count,
               is_deep_think,
               is_network_search,
               send_time
        from lmp.message
        where user_id = #{userId}
          and session_id = #{sessionId}
        order by send_time
    </select>

    <delete id="deleteByIds">
        delete
        from lmp.message
        where user_id = #{userId}
          and session_id = #{sessionId}
    </delete>
</mapper>