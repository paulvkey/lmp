<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.xjtu.springboot.mapper.ChatMessageMapper">
    <resultMap id="BaseResultMap" type="com.xjtu.springboot.pojo.ChatMessage">
        <id column="id" jdbcType="BIGINT" property="id"/>
        <result column="user_id" jdbcType="BIGINT" property="userId"/>
        <result column="session_id" jdbcType="BIGINT" property="sessionId"/>
        <result column="message_type" jdbcType="TINYINT" property="messageType"/>
        <result column="message_thinking" jdbcType="LONGVARCHAR" property="messageThinking"/>
        <result column="message_content" jdbcType="LONGVARCHAR" property="messageContent"/>
        <result column="type" jdbcType="VARCHAR" property="type"/>
        <result column="file_ids" jdbcType="VARCHAR" property="fileIds"/>
        <result column="token_count" jdbcType="INTEGER" property="tokenCount"/>
        <result column="is_deep_think" jdbcType="TINYINT" property="isDeepThink"/>
        <result column="is_network_search" jdbcType="TINYINT" property="isNetworkSearch"/>
        <result column="send_time" jdbcType="TIMESTAMP" property="sendTime"/>
    </resultMap>
    <delete id="deleteByPrimaryKey" parameterType="java.lang.Long">
        delete
        from chat_message
        where id = #{id,jdbcType=BIGINT}
    </delete>
    <insert id="insert" parameterType="com.xjtu.springboot.pojo.ChatMessage">
        <selectKey keyProperty="id" order="AFTER" resultType="java.lang.Long">
            SELECT LAST_INSERT_ID()
        </selectKey>
        insert into chat_message (user_id, session_id, message_type, message_thinking,
        message_content, type,
        file_ids, token_count, is_deep_think,
        is_network_search, send_time)
        values (#{userId,jdbcType=BIGINT}, #{sessionId,jdbcType=BIGINT},
        #{messageType,jdbcType=TINYINT},#{messageThinking,jdbcType=LONGVARCHAR},
        #{messageContent,jdbcType=LONGVARCHAR},#{type,jdbcType=VARCHAR},
        #{fileIds,jdbcType=VARCHAR}, #{tokenCount,jdbcType=INTEGER},
        #{isDeepThink,jdbcType=TINYINT},
        #{isNetworkSearch,jdbcType=TINYINT},
        #{sendTime,jdbcType=TIMESTAMP})
    </insert>
    <update id="updateByPrimaryKey" parameterType="com.xjtu.springboot.pojo.ChatMessage">
        update chat_message
        set user_id           = #{userId,jdbcType=BIGINT},
            session_id        = #{sessionId,jdbcType=BIGINT},
            message_type      = #{messageType,jdbcType=TINYINT},
            message_thinking  = #{messageThinking,jdbcType=LONGVARCHAR},
            message_content   = #{messageContent,jdbcType=LONGVARCHAR},
            type              = #{type,jdbcType=VARCHAR},
            file_ids          = #{fileIds,jdbcType=VARCHAR},
            token_count       = #{tokenCount,jdbcType=INTEGER},
            is_deep_think     = #{isDeepThink,jdbcType=TINYINT},
            is_network_search = #{isNetworkSearch,jdbcType=TINYINT},
            send_time         = #{sendTime,jdbcType=TIMESTAMP}
        where id = #{id,jdbcType=BIGINT}
    </update>
    <select id="selectByPrimaryKey" parameterType="java.lang.Long" resultMap="BaseResultMap">
        select id,
               user_id,
               session_id,
               message_type,
               message_thinking,
               message_content,
               type,
               file_ids,
               token_count,
               is_deep_think,
               is_network_search,
               send_time
        from chat_message
        where id = #{id,jdbcType=BIGINT}
    </select>
    <select id="selectAll" resultMap="BaseResultMap">
        select id,
               user_id,
               session_id,
               message_type,
               message_thinking,
               message_content,
               type,
               file_ids,
               token_count,
               is_deep_think,
               is_network_search,
               send_time
        from chat_message
    </select>

    <select id="selectBySessionId" parameterType="java.lang.Long" resultMap="BaseResultMap">
        select id,
        user_id,
        session_id,
        message_type,
        message_thinking,
        message_content,
        type,
        file_ids,
        token_count,
        is_deep_think,
        is_network_search,
        send_time
        from chat_message
        where session_id = #{sessionId}
        order by send_time
    </select>

    <delete id="deleteBySessionId" parameterType="java.lang.Long">
        delete
        from chat_message
        where session_id = #{sessionId,jdbcType=BIGINT}
    </delete>
</mapper>